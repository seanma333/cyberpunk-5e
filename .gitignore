# macOS
.DS_Store

# LevelDB runtime files (generated by Foundry when loading packs)
packs/**/*.ldb
packs/**/*.log
packs/**/LOG
packs/**/LOG.old
packs/**/LOCK
packs/**/CURRENT
packs/**/MANIFEST-*
packs/**/*.sst
packs/**/*.tmp

# Node
node_modules/

# Build output (if ever added)
dist/

# Temporary / working files
tmp/
